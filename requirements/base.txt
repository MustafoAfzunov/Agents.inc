Django==5.0.6
djangorestframework==3.15.1
requests==2.32.3
beautifulsoup4==4.12.3
trafilatura==1.12.0
python-dotenv==1.0.1
# >=1.55 required for compatibility with httpx>=0.28 (older versions pass a
# removed `proxies` kwarg and crash on client init).
openai>=1.55,<2
psycopg2-binary==2.9.9
# Production WSGI server + static file serving + DATABASE_URL parsing.
gunicorn==22.0.0
whitenoise==6.7.0
dj-database-url==2.2.0
# Offline fallback when OpenAI quota/key fails (LLM_PROVIDER=openai uses both).
spacy>=3.7,<3.8
